input {
  http {
    port => 5044
    codec => "json"
  }
}

output {
  elasticsearch {
    hosts => ["${ELASTICSEARCH_URL}:9200"]
    index => "client-service-logs-%{+YYYY.MM.dd}"
  }
  stdout { codec => rubydebug }
}
